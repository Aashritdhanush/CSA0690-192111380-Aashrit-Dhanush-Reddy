#include<stdio.h>
#include<conio.h>
int main()
{
int r,n,sum=0,temp;
printf("enter the number:");
scanf("%d",&n);
temp=n;
while(n>0)
{
r=n%10;
sum=sum+(r*r*r);
n=n/10;
}
if(sum==temp)
{
printf("it is an armstrong numbe");
}
else
{
printf("it is not an armstrong number");
}
return 0;
}
